query PostsGetAll ($limit: IntType! = 100) {
  allPosts(orderBy: [date_DESC], first: $limit) {
    __typename
    id
    title
    slug
    content {
      __typename
      value
    }
    date
  }
}
