query PostsGetBySlug($slug: String!) {
  post(filter: { slug: { eq: $slug } }) {
    __typename
    id
    title
    slug
    content {
      __typename
      value
      blocks {
        ... on PostGalleryRecord {
          __typename
          id
          images {
            __typename
            id
            url
            alt
            width
            height
            blurhash
          }
        }
      }
    }
    date
  }
}
